#spring.cloud.config.uri=http://104.211.212.28:51000
#spring.cloud.config.label=master
#spring.cloud.config.name=kernel
spring.application.name=mds
#management.security.enabled=false
#management.endpoint.health.show-details=when-authorized
#management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always
management.endpoints.web.exposure.include=info,health,refresh,mappings

server.port=8080
#server.servlet.path=/v1/mds

#disabling health check so that client doesnt try to load properties from sprint config server every
# 5 minutes (should not be done in production)
health.config.enabled=false

mosip.min-digit-longitude-latitude=4

spring.datasource.driverClassName=org.h2.Driver

javax.persistence.jdbc.driver=org.h2.Driver
javax.persistence.jdbc.url=jdbc:h2:mem:mosip_test;INIT=CREATE SCHEMA IF NOT EXISTS mosip_test;
javax.persistence.jdbc.user=root
javax.persistence.jdbc.password=root

spring.datasource.jdbcUrl=jdbc:h2:mem:mosip_test;INIT=CREATE SCHEMA IF NOT EXISTS mosip_test;
spring.datasource.username=root
spring.datasource.password=root